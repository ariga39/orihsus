Work in this repository and complete the task. You must perform a genuine three-round tool loop:

1. Read the implementation and tests, run the tests, and identify the first failure.
2. Fix only that failure, rerun the tests, then use the new failure as evidence for the next fix.
3. Fix the second failure, rerun, then fix the final failure and run the complete test suite once more.

Do not replace or weaken the tests. Keep the public API unchanged. The final implementation must correctly parse signed decimal integers, sum only valid lines, and report how many invalid lines were skipped.
